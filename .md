# ReCal-LM 150M

## Recurrent Calibration Language Model

**项目代号：** ReCal-LM
**第一阶段模型：** ReCal-LM-150M
**项目阶段：** P0 / P1 架构验证
**目标：** 验证“廉价循环状态更新 + 周期性完整 Transformer 校准”能否以更低平均计算量逼近标准 Decoder-Only Transformer。

---

# 1. 项目目标

传统 Decoder-Only Transformer 在生成每一个新 Token 时，新 Token 都必须依次经过整个模型：

[
x_t
\rightarrow E
\rightarrow L_1
\rightarrow L_2
\rightarrow \cdots
\rightarrow L_N
\rightarrow LMHead
]

KV Cache 可以避免历史 Token 的 Attention K/V 被重新计算，但新 Token 本身仍然需要经过全部网络层。

ReCal-LM 的核心假设是：

> 模型完成一次完整前向传播之后，可以得到一个包含当前上下文和预测状态的高维内部状态 (S_t)。在短时间尺度内，不必让每一个新 Token 都重新经历完整模型，而可以使用较小的循环网络 (R) 持续更新状态，并周期性调用完整网络进行校准。

因此目标不是消灭完整 Transformer，而是降低完整 Transformer 的调用频率。

---

# 2. 第一版总体架构

模型采用：

[
E\rightarrow A\rightarrow R\rightarrow B\rightarrow LMHead
]

其中：

**E — Embedding**

负责将 Token 转换为隐藏表示。

**A — Front Transformer**

负责从原始 Token 和上下文中提取较完整的语义表示。

**R — Recurrent Transformer**

项目的核心模块。

负责在两个完整校准点之间持续更新内部状态。

R 的参数在不同循环步骤之间共享。

**B — Back Transformer**

负责将内部状态进一步转换成适合语言预测的表示。

**LMHead**

输出下一个 Token 的概率分布。

---

# 3. ReCal-LM-150M 参数设计

第一阶段推荐配置：

| 参数                 |                       配置 |
| ------------------ | -----------------------: |
| Architecture       | Decoder-Only Transformer |
| Vocabulary         |                   32,000 |
| Context Length     |                    2,048 |
| Hidden Size        |                      768 |
| Attention Heads    |                       12 |
| Head Dimension     |                       64 |
| FFN Dimension      |                    3,072 |
| A Layers           |                        7 |
| R Layers           |                        4 |
| B Layers           |                        7 |
| Physical Layers    |                       18 |
| Activation         |                   SwiGLU |
| Normalization      |                  RMSNorm |
| Position Encoding  |                     RoPE |
| Embedding / LMHead |             Weight Tying |
| Precision          |                     BF16 |
| Dropout            |                    0～0.1 |

结构：

[
E
\rightarrow
A_1...A_7
\rightarrow
R_1...R_4
\rightarrow
B_1...B_7
\rightarrow
LMHead
]

参数量预计约：

[
150M\pm10M
]

最终参数量以实际 PyTorch `sum(p.numel())` 为准。

这里不强求数学意义上的恰好 150,000,000 参数。

---

# 4. Full Calibration 模式

完整模式执行：

[
X_{\le t}
\rightarrow
E
\rightarrow
A
\rightarrow
R
\rightarrow
B
\rightarrow
LMHead
]

同时生成新的内部状态：

[
S_t^{full}
]

以及正常的 KV Cache：

[
KV_t
]

完整计算承担三个作用：

1. 重新读取当前上下文；
2. 重建高质量内部状态；
3. 修正循环阶段积累的误差。

记为：

[
S_t=\operatorname{Full}(X_{\le t},KV_t)
]

---

# 5. Cheap Recurrent 模式

完成 Full Calibration 后，不立即再次运行完整模型。

循环网络读取：

[
S_t
]

当前 KV：

[
KV_t
]

以及刚刚生成的新 Token：

[
E(x_t)
]

执行：

[
S_{t+1}
=======

R(S_t,E(x_t),KV_t)
]

这里必须包含：

[
E(x_t)
]

原因是模型实际已经生成了新的 Token。

如果循环状态不知道刚刚输出了什么，那么内部状态会逐渐与真实生成序列脱离。

因此第一版正式定义为：

[
\boxed{
S_{t+1}=R(S_t,E(x_t),KV_t)
}
]

而不是单纯：

[
S_{t+1}=R(S_t,KV_t)
]

---

# 6. 循环过程

例如设置：

[
N=4
]

运行过程：

[
Full
\rightarrow
R^1
\rightarrow
R^2
\rightarrow
R^3
\rightarrow
R^4
\rightarrow
Full
]

然后重复。

第一阶段不训练自动 Calibration Gate。

只测试固定：

[
N\in{1,2,4,8}
]

必要时增加：

[
N=16
]

其中：

**N=1**

作为接近标准计算的对照。

**N=2**

测试极短循环。

**N=4**

第一阶段主要目标。

**N=8**

测试状态长期稳定性。

**N=16**

主要用于观察状态失真和崩溃点，不要求模型一开始就表现良好。

---

# 7. 第一阶段核心研究问题

ReCal-LM-150M 不追求聊天能力或 Benchmark SOTA。

第一阶段只回答以下问题：

### Q1

循环网络能否学习：

[
S_t\rightarrow S_{t+1}
]

而不是快速失真？

### Q2

随着循环次数增加：

[
R^1,R^2,R^4,R^8
]

模型性能下降速度是多少？

### Q3

一次 Full Calibration 是否能够重新将已经漂移的状态拉回正确分布？

### Q4

在相同语言建模质量下：

[
FLOPs_{ReCal}<FLOPs_{Baseline}
]

是否成立？

### Q5

ReCal-LM 的实际 Wall Clock 性能是否也提高，而不仅仅是理论 FLOPs 降低？

---

# 8. Baseline 模型

必须同时训练一个普通 Transformer Baseline。

Baseline 与 ReCal-LM 应尽量保持：

* 相同参数量；
* 相同 Tokenizer；
* 相同训练数据；
* 相同 Token 数；
* 相同 Optimizer；
* 相同 Learning Rate Schedule；
* 相同 Context Length。

Baseline 不使用循环：

[
E
\rightarrow
Transformer
\rightarrow
LMHead
]

目标是建立真正公平的：

[
Baseline\ vs\ ReCal
]

比较。

没有 Baseline，本实验的结果基本无法判断。

---

# 9. 第一阶段训练数据

目标训练量建议：

## P0

[
100M\sim300M\ Tokens
]

只验证代码和 Loss 是否正常。

## P1-A

[
1B\ Tokens
]

判断架构是否基本可行。

## P1-B

[
3B\ Tokens
]

作为150M主实验。

如果3B仍然持续改善，再扩到：

[
5B\ Tokens
]

第一阶段没有必要下载数百B Token。

---

# 10. 推荐数据组成

第一版推荐：

[
60%\ English\ Web
]

[
25%\ Chinese\ Web
]

[
10%\ Synthetic\ Educational
]

[
5%\ Code
]

对应3B Token：

| 数据                       |  Token |
| ------------------------ | -----: |
| FineWeb-Edu              |   1.8B |
| FineWeb-Edu-Chinese-V2.1 |   750M |
| Cosmopedia v2            |   300M |
| Python-Edu               |   150M |
| **总计**                   | **3B** |

---

# 11. 数据集一：FineWeb-Edu

数据集：

`HuggingFaceFW/fineweb-edu`

作为主要英文预训练数据。

FineWeb-Edu 当前公开版本约包含1.3T Token，并提供约10B、100B和350B Token 的较小采样版本，非常适合我们这种小模型实验。

第一阶段只需要：

[
1.8B Tokens
]

不需要完整下载数据集。

推荐通过 Streaming 或直接读取 sample-10BT 的部分 Parquet。

FineWeb-Edu 的 `sample-10BT` 当前约28.5GB，作为整个150M实验的数据源已经非常方便。

---

# 12. 数据集二：FineWeb-Edu-Chinese-V2.1

数据集：

`opencsg/Fineweb-Edu-Chinese-V2.1`

作为中文主要数据源。

该数据集是针对中文高质量教育语料筛选的数据集，目前 Hugging Face 数据页将其标记为10B～100B样本规模，并按照质量评分组织数据。

第一阶段只抽取：

[
750M Tokens
]

优先选择质量较高的部分。

注意：

该数据集除了 Apache-2.0 标记外，还有 OpenCSG Community License 条款；未来如果项目涉及商业使用，需要重新审查其具体许可条件。

研究实验阶段仍应保留完整数据来源记录。

---

# 13. 数据集三：Cosmopedia v2

数据集：

`HuggingFaceTB/smollm-corpus`

配置：

`cosmopedia-v2`

Cosmopedia v2 包含约3913万篇合成教材、博客和故事；SmolLM 资料称其约28B Token。

第一阶段抽取：

[
300M Tokens
]

它的主要作用并不是增加知识量，而是增加：

* 连贯长文本；
* 教材式解释；
* 连续语义结构；
* 因果关系；
* 故事状态。

这些数据特别适合测试循环状态能否在连续语境中保持信息。

---

# 14. 数据集四：Python-Edu

同样来自：

`HuggingFaceTB/smollm-corpus`

配置：

`python-edu`

Python-Edu 是从代码数据中筛选出的高教育质量 Python 内容。SmolLM公开资料给出的规模约为4B Token。

第一阶段仅加入：

[
150M Tokens
]

即总数据约5%。

目的不是训练代码模型，而是增加结构高度规则的数据：

[
function
\rightarrow condition
\rightarrow state
\rightarrow output
]

观察循环模型对结构化依赖的学习情况。

---

# 15. 为什么暂时不大量加入数据集

第一阶段最重要的是控制变量。

如果同时加入：

* 数学；
* GitHub；
* Wikipedia；
* Books；
* ArXiv；
* QA；
* SFT；
* CoT；

将很难判断模型性能变化来自数据还是架构。

因此 ReCal-LM-150M 只保留四类数据。

等架构成立以后再扩充。

---

# 16. Tokenizer

第一版自行训练统一 Tokenizer。

推荐：

**类型：**

SentencePiece BPE 或 HuggingFace Tokenizers BPE。

**Vocabulary：**

[
32,000
]

训练 Tokenizer 时按照正式训练数据比例取样：

[
60:25:10:5
]

保证中英文都具有合理 Token 效率。

特殊 Token 至少包含：

`<bos>`

`<eos>`

`<pad>`

`<unk>`

暂时不加入：

`<think>`

`<tool>`

`<assistant>`

等聊天模型 Token。

因为第一阶段训练的是 Base LM。

---

# 17. Sequence Length

第一阶段：

[
Context=2048
]

暂时不做：

[
4096/8192/32768
]

因为研究目标不是 Long Context。

2048 足够观察：

* KV；
* 状态漂移；
* 循环稳定性；
* Calibration；
* Language Modeling Loss。

同时显著降低训练成本。

---

# 18. 第一版训练 Loss

至少包含三个目标。

## 18.1 Language Modeling Loss

正常语言模型目标：

[
L_{LM}
======

-\sum_t
\log P(x_{t+1}|x_{\le t})
]

这是主要任务。

---

## 18.2 State Distillation Loss

同一位置分别计算：

[
S^{full}
]

和：

[
S^{loop}
]

要求循环状态逼近完整计算状态：

[
L_{state}
=========

1-\cos
(S^{loop},stopgrad(S^{full}))
]

第一版优先使用 Cosine Loss。

后续再实验：

[
MSE
]

和：

[
Normalized\ MSE
]

---

## 18.3 Logit Distillation Loss

让廉价模式输出：

[
P_{loop}
]

逼近完整计算：

[
P_{full}
]

使用：

[
L_{KD}
======

KL(
P_{full}
||
P_{loop}
)
]

最终：

[
\boxed{
L=
L_{LM}
+
\lambda_sL_{state}
+
\lambda_kL_{KD}
}
]

初始建议：

[
\lambda_s=0.1
]

[
\lambda_k=0.5
]

之后根据梯度尺度调整，而不是长期固定这些数值。

---

# 19. 循环训练策略

训练时不要永远固定：

[
N=4
]

而随机选择：

[
N\in{1,2,4,8}
]

例如：

[
P(N=1)=0.20
]

[
P(N=2)=0.25
]

[
P(N=4)=0.35
]

[
P(N=8)=0.20
]

这样可以避免 R 只学会某个固定循环深度。

---

# 20. 第一阶段不实现的功能

ReCal-LM-150M v0.1 明确不实现：

* 无限循环；
* 自适应 Calibration Gate；
* 强化学习；
* Chat SFT；
* RLHF；
* MoE；
* 多模态；
* 长期记忆；
* 复杂外部 Memory；
* 7B；
* FP8；
* 多机训练。

这些全部延后。

第一版只验证最重要的：

[
\boxed{
短期循环状态是否成立
}
]

---

# 21. 训练阶段

## Stage P0 — Smoke Test

模型：

ReCal-LM-20M / 50M

Token：

[
100M
]

目的：

检查：

* Forward；
* Backward；
* KV；
* Loop；
* Calibration；
* Checkpoint；
* Resume；
* Docker；
* Loss。

成功后立即停止，不追求模型质量。

---

## Stage P1-A — 150M Pilot

模型：

ReCal-LM-150M

数据：

[
1B Tokens
]

训练：

Baseline + ReCal。

主要判断：

[
N=1,2,4,8
]

是否出现明显性能规律。

---

## Stage P1-B — 150M Main

如果1B实验成立：

[
3B Tokens
]

正式进行完整实验。

---

# 22. 必须记录的指标

每次实验至少保存：

### 语言质量

* Train Loss
* Validation Loss
* Perplexity

### 循环稳定性

[
D(S^{loop}_1,S^{full})
]

[
D(S^{loop}_2,S^{full})
]

[
D(S^{loop}_4,S^{full})
]

[
D(S^{loop}_8,S^{full})
]

### 输出分布

[
KL(P_{full},P_{loop})
]

### 计算成本

* FLOPs / Token
* GPU Time / Token
* Tokens / Second
* Peak VRAM
* KV Cache Size

### Calibration 效果

重点记录：

[
Error_{before\ calibration}
]

和：

[
Error_{after\ calibration}
]

如果 Full Calibration 无法显著降低状态误差，则当前架构基本失败。

---

# 23. 最重要的实验图

最终至少生成：

### Figure A

[
Loop\ Steps
\quad vs\quad
Validation\ Loss
]

### Figure B

[
Loop\ Steps
\quad vs\quad
State\ Error
]

### Figure C

[
Relative\ FLOPs
\quad vs\quad
Model\ Quality
]

### Figure D

Calibration 前后：

[
State\ Error
]

变化。

整个项目最重要的结果最终应该是一条：

[
\boxed{
Quality\ vs\ Compute
}
]

Pareto Curve。

---

# 24. 成功标准

第一阶段不要求 ReCal 击败普通 Transformer。

如果能够达到：

[
70%\sim80%
]

的实际计算量，

同时保持：

[
\ge95%
]

的 Baseline 语言建模能力，就已经值得进入1B阶段。

更加理想的结果：

[
50%\sim60%\ Compute
]

保持：

[
\ge95%
]

模型能力。

---

# 25. 失败标准

如果出现：

[
R^1
]

尚可，

但是：

[
R^2,R^4
]

迅速崩溃；

或者 Full Calibration 后：

[
Error
]

不能重新下降；

则暂停扩大模型。

优先重新研究：

* State 表示；
* R 输入；
* R 与 KV 的交互；
* State Distillation；
* Calibration 方法。

绝不通过直接扩大到7B掩盖架构问题。

---

# 26. 推荐项目目录

```text
recal-lm/
├── configs/
│   ├── recal_50m.yaml
│   ├── recal_150m.yaml
│   └── baseline_150m.yaml
│
├── recal/
│   ├── model/
│   │   ├── embedding.py
│   │   ├── attention.py
│   │   ├── transformer.py
│   │   ├── recurrent.py
│   │   ├── recal_model.py
│   │   └── baseline.py
│   │
│   ├── training/
│   │   ├── trainer.py
│   │   ├── losses.py
│   │   ├── scheduler.py
│   │   └── checkpoint.py
│   │
│   ├── data/
│   │   ├── dataset.py
│   │   ├── tokenizer.py
│   │   ├── packing.py
│   │   └── mixture.py
│   │
│   └── evaluation/
│       ├── perplexity.py
│       ├── state_drift.py
│       ├── calibration.py
│       └── benchmark.py
│
├── scripts/
│   ├── train_tokenizer.py
│   ├── prepare_data.py
│   ├── train.py
│   ├── evaluate.py
│   └── profile.py
│
├── docker/
│   └── Dockerfile
│
├── tests/
│   ├── test_model.py
│   ├── test_recurrent.py
│   ├── test_kv_cache.py
│   └── test_checkpoint.py
│
├── experiments/
│
├── README.md
└── pyproject.toml
```

---

# 27. 云端训练原则

Docker 镜像只包含：

* CUDA Runtime；
* PyTorch；
* Flash Attention；
* HuggingFace Datasets；
* Tokenizers；
* 项目源码；
* Python依赖。

Docker 镜像不包含：

* 训练数据；
* Checkpoint；
* 实验结果。

训练服务器流程：

[
Docker\ Image
]

[
+
]

[
Remote\ Dataset
]

[
+
]

[
Remote\ Checkpoint
]

这样可以先在廉价 GPU 上完成环境搭建与调试，然后直接将同一个镜像迁移至高性能 GPU。

---

# 28. 第一里程碑

ReCal-LM 第一个真正里程碑不是“训练完成150M模型”。

而是得到下面这张实验表：

| Model    | Loop | Val Loss | Relative FLOPs | tok/s | State Drift |
| -------- | ---: | -------: | -------------: | ----: | ----------: |
| Baseline |    — |        — |           1.00 |     — |           — |
| ReCal    |    1 |        — |              — |     — |           — |
| ReCal    |    2 |        — |              — |     — |           — |
| ReCal    |    4 |        — |              — |     — |           — |
| ReCal    |    8 |        — |              — |     — |           — |

只要这张表能够证明：

[
Loop\uparrow
]

时，

[
Compute\downarrow
]

而：

[
Quality
]

没有同比例下降，

ReCal-LM 的核心假设就获得了第一份实验支持。

---

# 29. 项目的一句话定义

> **ReCal-LM 是一种使用短周期循环隐藏状态进行低成本连续预测，并通过周期性完整 Transformer 前向传播重新校准内部状态的语言模型架构。**

核心研究目标：

[
\boxed{
减少重复深层计算，而不是减少模型拥有的知识和表示能力。
}
]
